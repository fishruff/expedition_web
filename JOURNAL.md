# Дневник работы — Expedition Web

Хронология работы над сайтом Minecraft-сервера Expedition: что сделано, какие решения приняты
и что в очереди. Новые записи добавляем **сверху**, в формате `## YYYY-MM-DD — заголовок`.

---

## 2026-08-24 — Плагин написан и проверен на живом сервере

Шесть этапов из семи закрыты: очередь, присутствие, статистика, метки и находки,
записи книгой, зоны и ручные рычаги. Остался седьмой — докер.

### Проверено не сборкой, а событиями

На настоящем Paper с настоящим миром прошли **все девять типов событий контракта**.
Это видно не по логам плагина, а по журналу `api` и по снимкам, которые читает сайт:
экипаж наполнился, архив получил находки, хронометр открылся, метки легли на карту,
записи книгой появились в дневнике.

Отдельно стоит записать три вещи, которые выглядят поломкой, но ею не являются —
чтобы в следующий раз не искать ошибку там, где её нет.

- **Прочтение своей же находки не считается.** `record.read` приходит, а `readBy`
  остаётся нулём: нашедший в счётчике прочитавших не участвует, он показан отдельной
  строкой. Правило из контракта сработало ровно как написано.
- **Метки на карте копятся, пока сама карта заперта.** `places` растёт, но раздел
  открывается своим ключом `map`, как хронометр — ключом `chronometer`. Игрок ходит
  по острову, а карта потом объясняет, где он был.
- **События «вышел» может не быть при остановке сервера.** Bukkit гасит плагины
  раньше, чем отключает игроков. Онлайн от этого не страдает: он считается по сигналу.

### Что нашла живая проверка, а рассуждение — нет

- **Bukkit режет аргументы команды по пробелам.** `/expedition place южный берег`
  открывало место «южный», молча потеряв хвост, а проверка «в номере не должно быть
  пробелов» не срабатывала никогда — до неё пробелы уже не доходили.
- **`api` было нечем запустить.** `node api/src/main.ts` из README падает: Node из
  репозитория Ubuntu собран без поддержки TypeScript. Команда, которой я обещал
  отлаживать плагин, не работала ни у кого. Поставлен `tsx`, теперь `npm run api`.
  `data:sample` был сломан ровно так же.
- **Плагин не собирался** с самого начала: поле стояло снаружи класса, и Java 21
  приняла файл за unnamed class — сообщение уводит совсем не туда, где ошибка.
- **`box-shadow` без размытия у прозрачного блока** заливает весь блок, а не рисует
  линию сбоку. Сгиб разворота переделан на псевдоэлемент.

### Offline-режим виден вживую

Сервер в `online-mode=false`, поэтому `uuid` вычисляется из ника. Владелец зашёл под
ником, под которым на этом мире ещё не играл, — и статистика честно показала нули,
хотя в мире лежат файлы с сорока минутами игры под другими никами. Это не ошибка
склейки, а прямое следствие: **смена ника равна смене человека**. К старту ники надо
зафиксировать и внести в `white-list`.

### Долги, которые видно прямо сейчас

- **Идентификаторы разошлись.** Из игры пришли `hram_1` и `hram`, а в `story.json`
  лежат `храм-1`, `храм-2`, `гавань-1`. Находки показываются фактом без текста —
  описанный крайний случай, но текста не будет, пока id не совпадут. То же с местом
  `пляж`, которого нет в `places.json`.
- **Ключ `map` не выдан**, поэтому раздел карты заперт при трёх накопленных метках.
- **Зона пока одна** — «южный берег» вокруг (50, 470). Остальные ждут облёта мира.
- **`white-list=false`**, плагина пароля нет. Единственное, что закрывает вход
  в offline-режиме.
- Положение метки «южный берег» на картинке карты (`x`, `y` в долях) поставлено
  на глаз, как и её название с описанием.

### Дальше

Седьмой этап: сервис `api` в `docker-compose.yml`, общая сеть, `EXPEDITION_KEY`
из `.env`, том со снимками, переезд `server/` в репозиторий. Мешает то, что docker
из WSL недоступен — демон живёт в Docker Desktop без интеграции, поэтому контейнер
перезапускается руками через его интерфейс.

---

## 2026-08-19 — ТЗ владельца сведено со спекой

Владелец написал собственное техническое задание на `ExpeditionCore` и попросил разобрать.
Разбор дал больше, чем правки: две вещи оттуда были нереализуемы, зато одна посылка, которую
мы ни разу не обсуждали, изменила контракт.

### Offline-режим ломает проверку самозванца

Главное, что принесло ТЗ: **сервер работает в `online-mode=false`** — часть игроков приходит
с пиратских клиентов. Мы этого не знали, а контракт стоял на фразе «`uuid` — главный ключ,
ник может смениться, `uuid` никогда».

В offline-режиме всё наоборот: идентификатор не выдаётся Mojang, а **вычисляется из ника**
(`UUID.nameUUIDFromBytes("OfflinePlayer:" + ник)`). Он детерминированный.

Из этого следует то, ради чего в ТЗ была целая подсистема: проверка «зашли под знакомым ником,
но с другим `uuid` — значит самозванец» **не может сработать никогда**. Настоящий игрок
и самозванец получают один и тот же идентификатор, байт в байт. Различать там нечего.

Решение владельца: вход закрывают ванильный `white-list` и сторонний плагин пароля, а телеграм-бот
остаётся в запасе. Плагин авторизацией не занимается вовсе — и не только из экономии: **подсистема
входа умеет запереть всех снаружи**, и держать её в одном плагине с телеметрией находок значит,
что ошибка в подсчёте блоков может стоить сезона.

Для склейки снимков `uuid` при этом годится: список участников закрыт, ники не меняются. Но теперь
это записано, а не подразумевается.

### Кириллический ключ открывал бы несуществующий раздел

Вторая находка нашлась при сверке идентификаторов. Контракт приводил в примере
`"artifactId": "хронометр"`, а сайт проверяет ключ буквально — `isUnlocked(unlocks, 'chronometer')`.
`api` кладёт ключ в разблокировки **как есть**, не заглядывая в сюжет. То есть хронометр
открывался бы ключом `хронометр`, а раздел ждал `chronometer`, и это молчаливый отказ:
ни ошибки, ни следа в логе.

Показательный сезон в `tools/sample` уже писал латиницу — то есть код втихую обошёл документ,
иначе раздел не открывался бы и в разработке. Правило записано: `artifactId` латиницей и равен
имени раздела. Для `recordId` и `placeId` это лишь рекомендация — там обе стороны авторские,
и они сверяются только друг с другом.

### Что из ТЗ выброшено и почему

- **PostgreSQL** — базы нет и не будет, решено ещё 15 августа и уже реализовано журналом `jsonl`.
- **Прирост вместо слепка** («накопили +1 и отправили за 30 секунд») — прирост требует, чтобы
  не потерялось ни одно событие; одна потеря, и число врёт до конца сезона.
- **Половина статистики вообще не нужна**: ваниль сама считает добытое, убитых и время, и её
  счётчики переживают падение сервера, а `sessionStart`/`sessionEnd` из ТЗ — нет. Своими руками
  считаем только поставленные блоки: их ванильного счётчика не существует.
- **Кеш «а `lore_017` уже найден?»** — плагин спрашивал бы `api`, а при недоступном `api` кеш
  устаревает, и двое игроков оба становятся первооткрывателями. Первенство уже решает `api`
  по полю `at`, и для этого не нужно ни спрашивать, ни кешировать. Заодно уцелела односторонняя
  связь: плагин по-прежнему только шлёт.
- **`InventoryPickupItemEvent`** — это событие воронки, а не игрока. Ровно тот случай, ради
  которого мы выбрали обход инвентарей: путей, которыми предмет попадает в рюкзак, семь.
- **`BLOCK_MINED` и `MOB_KILLED` как события** — это не события, а статистика; они забили бы
  журнал за час.

### Что из ТЗ взято

**Зоны.** Прямоугольники в конфиге, имена из `places.json`, событие при входе. Оказалось,
что контракт менять не нужно: `place.revealed` уже существовал ручным рычагом, а `api` не
различает, кто его послал. Проверка — раз в секунду по тем, кто в сети, как и обход инвентарей.

Цена ванильной статистики названа честно: **отфильтровать её нельзя**. Исключить из «блоков
добыто» служебные конструкции не получится. По-моему, это хвастовство, а не аудит.

Мобы по типам отложены: ваниль хранит `KILL_ENTITY` всегда, но на сайте под них нет ни места,
ни решения — а заводить механику ради красивой цифры мы уже отказывались.

---

## 2026-08-17 — Плагин спроектирован, репозиторий стал общим

### Три решения владельца

- **Сюжетная книга узнаётся по метке внутри предмета**, а не по координатам сундука и не по
  заголовку. Ключ кладётся в `PersistentDataContainer` командой оператора. Метка едет вместе
  с предметом — через сундуки, смерть, наковальню; координаты сундука врут, как только книгу
  вынесли, а заголовок может подделать любой игрок и открыть себе раздел.
- **Paper 1.21, Java 21.** Совпало с тем, что уже стояло в `site.ts` и в докере.
- **Предметы раскладывает владелец руками** до старта. Плагин умеет только метить и слушать —
  подсистемы раскладки по конфигу координат не будет.

### Находки ловятся обходом инвентарей

«Предмет попал в инвентарь» — это семь разных событий Bukkit: клик, перетаскивание, подбор
с земли, подбор после смерти, шифт-клик, обмен с хотбаром. Пять напишешь, шестой забудешь,
и случится именно он.

Поэтому обход раз в пять секунд: один путь вместо семи, и он проверяем. Задержка ничего не
стоит — `api` пересобирает снимки раз в минуту, а очерёдность считает по полю `at`, а не по
порядку прихода.

Обход **пропускает творческий режим**, иначе владелец, раскладывающий книги перед сезоном,
«найдёт» их все сам.

### Что вскрыла сверка спеки с кодом приёмника

Писал не по контракту, а по `api/src` — и три вещи нашлись только так:

- **Чистить очередь по списку `accepted` нельзя.** `api` возвращает в нём только номера,
  которых раньше не видел. Повтор доедет с `200` и пустым `accepted`, и плагин, доверяющий
  этому полю, зациклится на первом же повторе. Правило: `200` — вся пачка доставлена.
- **Потеря состояния плагина снимки не портит.** Повторная находка превращается в прочтение,
  а прочтение нашедшим не засчитывается. Это следует из кода, а не из предположения.
- **`/note` обязан метить опубликованную книгу**: номер записи на сайте берётся из номера
  события, значит вторая команда той же книгой даст два одинаковых дневника.

Плюс дыра, которую видно только при сведении двух документов: ручной рычаг для меток на карте
есть (`place.revealed`), а для разблокировки раздела — нет. Утонул артефакт в лаве — хронометр
открыть нечем. Добавлена команда `/expedition unlock`, контракт при этом не меняется.

И честная находка про сам `api`: **пределы записей в нём не реализованы**. Контракт обещает
отклонять книги длиннее пятидесяти страниц и двадцать первую запись за сутки, но проверяются
только четыре обязательных поля. Сторожем пока оказывается плагин — а сторож, которого обходят
переустановкой плагина, не сторож.

### Репозиторий теперь про всю систему, а не про сайт

Плагин жил отдельной папкой вне гита. Сведён сюда, и причина ровно одна: `api/src/events.ts` —
это зеркало того, что шлёт плагин. Добавили поле в событие — обязаны поменять обе стороны.
В одном репозитории это один коммит, и забыть невозможно; в двух правило «контракт нельзя
менять в одностороннем порядке» держится на памяти, а память подводит через месяц.

**Сайт при этом не двигали ни на файл.** Была мысль увести его в `website/` ради симметрии трёх
частей — от неё отказались: ценность в одном репозитории, а не в глубине папок, и платить за
косметику переносом двух десятков файлов незачем. Цена решения названа: в корне рядом с `plugin/`
лежит `src/`, и то, что это сайт, приходится объяснять в README.

Сервер (`docker-compose.yml` и мир) пока остался снаружи: данные мира — 346 МБ, их мог держать
запущенный контейнер. Переедет отдельным шагом, вместе с добавлением в compose сервиса `api` —
сейчас его там нет, а плагину нужно имя `api` внутри докерной сети.

### Плагин не собирался

`private ConfigManager configManager;` стояло снаружи класса. Java 21 приняла файл за unnamed
class и ругалась на preview-фичу — сообщение уводит совсем не туда, где ошибка. Одна строка.

Следствие: `jar` на сервере собран **до** появления `ConfigManager` — в старой сборке его класса
просто нет. После починки собирается целиком, конфиг лежит внутри.

Спека: [`docs/superpowers/specs/2026-08-17-expedition-core.md`](docs/superpowers/specs/2026-08-17-expedition-core.md).

### Дальше

Каркас по спеке: конфиг, очередь на диске, HTTP-клиент. Их можно проверить против живого `api`
до того, как на сервер зайдёт хоть один игрок.

---

## 2026-08-17 — Решения владельца: сайт зафиксирован, очередь за плагином

### Что решено

- **Главная — это дневник.** Титульная страница с адресом сервера убрана: человек приходит
  смотреть, что случилось, а не читать обложку. Маршруты приведены к решению владельца:
  `/` дневник, `/map`, `/archive`, `/players`, `/players/:nick`, `/chronometer`, `/about`.
  Отдельного `/log` больше нет.
- **Адрес сервера уехал в подвал** и стал кнопкой: по клику копируется. В композиции стола
  он был лишним, но игроку нужен на каждой странице.
- **Достижения не делаем.** Абстрактные «Исследователь I» требуют системы условий и новых
  событий от плагина, а открывают они ничего. В карточке остаются только настоящие данные.
- **Статистика зафиксирована пятью показателями**: блоков добыто, мобов убито, времени
  в игре, записей найдено, артефактов найдено. Чанки не реализуем — механику отменили ещё
  в контракте, и красивая цифра в мокапе не повод её возвращать.
- **Найденные артефакты** взялись бесплатно: в снимке разблокировок у каждого ключа уже
  записано время и имя нашедшего.

### Правило, которое стоит помнить

**Мокап — не техническая спецификация.** Если на картинке появился красивый элемент,
которого нет в контракте, он не реализуется автоматически. Иначе картинка начинает
диктовать механику, а мы уже один раз отменяли чанки и чуть не вернули их обратно.

### Уточнение по архитектуре

В плане работ владельца шагом стоит «Database». Её у нас нет и не будет: при пятнадцати
игроках и шести неделях данные измеряются килобайтами, роль базы играют JSON-снимки,
которые api перезаписывает раз в минуту. Отдельного этапа под хранилище не требуется.

### Очередь

Приоритет владельца: ExpeditionCore, затем api, затем интеграция, и только потом сайт.
Внутри этого пункта api поднимается первым — плагин без приёмника не отладить, а ядро
приёмника уже написано и покрыто тестами в `tools/sample/apply.ts`.

---

## 2026-08-16 — Первые нарисованные ассеты и две сетки

Владелец перерисовал компас и часы руками по эскизам нейросети — и это оказалось верным
решением. `pixelize.py` прореживает готовый рендер, то есть выбирает, какие точки выбросить;
рука решает это бесплатно и точнее.

### Две сетки вместо одной

Рисунок часов не влезал в задуманные 48 точек: стрелки в одну точку толщиной при уменьшении
исчезали. Ужимать рисунок ради канона неправильно — канон существует ради качества, а не
наоборот.

Поэтому сеток стало две. **Интерфейсная** — рамки, иконки, шрифт, `--s`. **Предметная** —
предметы, арты, карта, `--sa`, всегда половина от `--s`. Так устроен и референс: панели там
простые, предметы подробные.

Цена названа сразу: ступеней масштаба стало две вместо четырёх, потому что половина от
нечётной ступени дробная. На мониторе 1920 сцена теперь такая же, как на 1280.

### Чему научились на промптах

Первая версия промптов давала предметы **сбоку, в перспективе**. Причина оказалась в словах
`lying on a table, seen from above` — для генератора это прямое указание строить перспективу.
Лечится ортогональной проекцией: `flat orthographic front view, no perspective, no ground`.

Но сильнее любых прилагательных работает **приложенная картинка**. Собран лист-эталон
`docs/art/reference/style-sheet.png`: нарисованные компас и часы в четырёхкратном увеличении
плюс палитра плашками. Генератор видит величину точки и оттенки своими глазами.

Ещё канон поправлен по факту: **тень в спрайт не запекается**. Оба нарисованных предмета
сделаны без неё, и это правильнее — запечённая тень воюет с фоном и мешает обрезке полей.

### Что подтвердилось цифрами

Оба присланных файла пришли с бинарной альфой и честной сеткой один к одному. Цвета попали
в канон почти без правки: у часов 2.2% точек дальше тридцати единиц от палитры, у компаса
3.1%. После привязки осталось 26 и 27 цветов вместо 3500 и 2835 — картинки при этом не
изменились. То есть палитра, снятая с прежних ассетов, совпала с тем, что владелец рисует
на глаз.

### Экипаж на авторских данных

Первый настоящий раздел: `/crew` и `/crew/:nick`. Слой данных и склейка были написаны
раньше и ждали потребителя, поэтому раздел собрался почти целиком из готового.

Что решено по ходу:

- **Звания.** Автоматические считаются по `titles.json`: звание достаётся первому по
  показателю. При ничьей — всем, кто делит вершину: отбирать награду у обоих из-за
  совпадения несправедливее, чем выдать дважды. Нулевой показатель звания не даёт, иначе
  «первый по нулю» обесценит остальные. Авторское звание из `crew.json` важнее
  автоматического — владелец проставил его осознанно.
- **Молчание вместо выдумки.** Пока снимков нет, интерфейс не пишет ни «в сети», ни
  «не в сети»: второе такое же враньё, как первое. Статистика в этом случае — прочерк
  и подпись «данных пока нет», а не нули.
- **Неописанный участник сохраняет звание.** Первая версия карточки заменяла звание
  пометкой «ещё не представился», и заслуженная в игре награда пропадала. Тест поймал
  это раньше глаз.
- **Поле `art` убрано из `crew.json`.** Путь к арту выводится из ника, а поле осталось
  от прежней схемы: владелец заполнил бы его, и ничего бы не произошло. Мёртвое поле,
  которое выглядит рабочим, хуже отсутствующего.

### Решения

- **Плагин ExpeditionCore пишу я**, но не сейчас — по команде владельца.
- Оставшиеся предметы (лампа, сундук, свиток, иконки) — декор, разделы от них не зависят:
  `Sprite` подставляет силуэт нужного размера. Поэтому разделы собираются параллельно
  с рисованием, а подключение готового файла стоит одной строки в реестре.

---

## 2026-08-15 — Контракт проверен кодом, маршруты сведены со спекой

### Показательный сезон вместо выдуманных JSON

Контракт был на бумаге: четыреста строк договорённостей между двумя ещё не написанными
сторонами. Вместо того чтобы руками сочинить пять файлов снимков, написан `tools/sample` —
**сведение потока событий в снимки**, то есть та самая работа, которую во время сезона будет
делать `api`. Проверяется весь контракт целиком, а не половина, и у плагина появляется эталон,
с которым можно сверить вывод.

Запускается `npm run data:sample`, кладёт в `public/data` правдоподобную неделю: экипаж, находки,
записи книгой, разблокировки. Всё считается от момента запуска, поэтому данные всегда свежие,
и ни одного случайного числа — одинаковый запуск даёт одинаковый сезон.

**Файлы снимков в репозиторий не попадают.** Они лежат в `public/`, а значит, уехали бы в
продакшн-сборку: сайт показал бы игрокам выдуманный сезон как настоящий.

### Что вскрыла проверка

Пять мест, где контракт молчал или расходился сам с собой. Все закрыты тестами и дописаны
в `api-v1.md`:

- **Ключ разблокировки — это `artifactId`.** Документ говорил «что откроется, решает сайт по
  `story.json`», но `unlocks.json` пишет `api`. Разошлись: `api` кладёт ключ как есть, а что за
  ним стоит, знает только сайт.
- **Даты сезона событиями не приходят** — их знает настройка `api`, не игра.
- **Список онлайна пустеет вместе с протухшим сигналом.** Иначе снимки противоречат друг другу:
  «сервер выключен», но в экипаже кто-то в сети. Первая версия сведения этим и болела.
- **Нашедший запись не считается прочитавшим её** — он показан отдельной строкой.
- **Архиву не нужен отдельный ключ**: он открывается самим фактом первой находки.

### Маршруты и замки

Пути разошлись со спекой ещё в прошлой записи: жил `/news` из концепции лендинга, не было
`/log`, `/archive`, `/chronometer`. Сведены со спекой, `JOURNAL_ENTRIES` (оглавление кожаного
дневника) удалён как наследство отменённой концепции.

Появился `Gate` — замок на разделе. Правило открытия живёт в одном месте (`src/data/unlocks.ts`),
а не размазано по разделам. Карты и хронометра нет в навигации намеренно: они живут предметами
на столе, компасом и часами, и там же показываются запертыми.

Тестов стало 101 вместо 57.

### Дальше

- [ ] Разделы на авторских данных: экипаж, участник, дневник, архив, устав, карта.
- [ ] Авторские файлы по спеке: `story.json`, `places.json`, `titles.json`, `charter.json`.
- [ ] **Вопрос владельцу:** плагин пишет он или я? От ответа зависит, что делать раньше.
- [ ] **Вопрос владельцу:** в `site.ts` до сих пор плейсхолдеры вместо адреса и версии.

---

## 2026-08-15 — Проект оказался больше сайта

Владелец описал продукт целиком, и картина изменилась: это не сайт-визитка сервера, а **вторая
половина игры**. Сезонный сервер на 5–15 человек, сюжетная неделя, история через найденные книги
и артефакты, сайт раскрывается вместе с игроками.

### Что решили по архитектуре

Три части: Minecraft с плагином ExpeditionCore, сервис приёма событий, сайт. Всё на одной машине
в контейнерах докера. Обмен — по HTTP внутри докерной сети.

Решения, которые стоит помнить, потому что они неочевидны:

- **Базы данных не будет.** Пятнадцать игроков, шесть недель, история между сезонами не нужна —
  объём данных измеряется килобайтами. Postgres тут только добавил бы точек отказа. Всё на JSON.
- **`season_id` не нужен** по той же причине: прошлые сезоны не хранятся.
- **Обмен через HTTP, а не через общий том**, хотя всё на одной машине. Контейнеры с Java и Node
  работают под разными пользователями и передерутся за права на файлы. Классические докерные грабли.
- **Онлайн берём из ежеминутного сигнала**, а не из событий входа и выхода. Если сервер упадёт,
  событие «вышел» не придёт и сайт навсегда покажет людей в игре. Сигнал чинит это сам за минуту.
- **Статистику шлём слепком, а не приростом.** Прирост требует, чтобы не потерялось ни одно
  событие; слепок не требует ничего — последний перекрывает предыдущий.
- **Первенство находки решает сервис приёма**, не плагин и не сайт. Первое пришедшее событие
  побеждает, остальные становятся «прочитал».
- **Исследованные чанки не отслеживаем.** Владелец предложил обойтись пройденным расстоянием,
  которое ваниль считает сама. Следствие: туман войны на карте отменяется, карта открывается
  **метками мест**, а не территорией. Дешевле и повествовательно сильнее.
- **Записи игроков пишутся книгой с пером в игре**, командой `/note`. Чат ограничен 256 символами
  без переносов — для дневника не годится. Книга снимает ограничение и не требует авторизации
  на сайте: сайт остаётся только для чтения, а это радикально дешевле и безопаснее.

### Что сделано

- Написан **контракт обмена** `docs/contract/api-v1.md`: события от плагина, снимки для сайта,
  файлы владельца, поведение при сбоях. Он нужен раньше кода, потому что от него зависят обе стороны.
- Спека переписана под продукт целиком: `2026-08-15-expedition-design.md`. Прежняя, узкая,
  отправлена в архив.

### Сроки

Старт сезона примерно через два месяца. Заложена **репетиция за неделю до старта**: сюжетная
часть длится неделю и повторить её нельзя, поэтому сбои надо найти заранее.

---

## 2026-08-14 — Настоящие ассеты и первая сцена

### Что сделано

Владелец прислал первые сгенерированные ассеты: компас, карманные часы, текстуру дерева. Все три
обработаны и стоят в проекте. Собрана первая сцена на настоящих ассетах: стол, предметы, панели,
живой отсчёт, навигация.

### Чему научились на ассетах

**Генератор не выдаёт настоящий пиксель-арт.** Компас пришёл в 1024 точки с 7129 цветами и
сглаженными краями — перебор всех размеров блока показал, что пиксельной сетки нет вообще.
Написан `tools/pixelize.py`: прореживание до родного размера, медианное сечение палитры, жёсткая
альфа, обрезка полей. Результат — 82×65 точек и 20 цветов вместо мегабайта.

**Квантование по частоте съедает редкие цвета.** Первый прогон потерял красную стрелку компаса —
её пикселей мало, они не попали в топ. Переписано на медианное сечение, которое делит цветовое
пространство, а не считает частоты.

**Фон бывает заперт внутри контура.** У часов заливка от краёв не добралась внутрь кольца цепочки —
пришлось добавить второй проход по всему изображению.

**О вырезанном предмете нельзя судить на белом фоне.** Потратил два подхода на «светлый ореол»
у часов и чуть не испортил цепочку, а на настоящем тёмном фоне ореол исчез сам — это была иллюзия
от контраста.

**Повторение тайла лечится не артом.** Дерево пересобрано из зеркальных копий: период вчетверо
длиннее при точных стыках. Важная тонкость — отражения обязаны зависеть только от номера строки
и столбца, иначе ломаются перпендикулярные швы (первая попытка это и сделала). Но главное
лекарство — **неповторяющийся слой света** поверх сцены.

**Рамки проще нарисовать кодом, чем сгенерировать.** Их стороны обязаны быть однородными по всей
длине, а модели рисуют уникальный завиток посередине. Рамки перерисованы кодом по палитре,
снятой с настоящих ассетов.

### Найденная ошибка в основании

Единица пиксельной сетки стояла 4 пикселя, из-за чего `px(4)` давал 48 экранных пикселей вместо
четырёх при масштабе 3 — все размеры были вчетверо крупнее задуманного. Исправлено: единица равна
одной точке исходника.

---

## 2026-08-13 — Смена оформления: пиксельное направление

Фотореалистичный дневник отменён. День ушёл на прототипы и проверку гипотез — ниже, что выяснилось
и почему в итоге свернули туда, куда свернули.

### Что пробовали и почему не вышло

**Фото плюс CSS-текст.** Собрали сцену из сгенерированных фоторендеров, текст клали поверх блендом
`multiply`, с подгонкой под изгиб страниц. Выглядело как наклейка на фотографии. Причина не в
настройках: референс, на который ориентировались, — это сгенерированная **картинка сайта**, где
текст нарисован той же моделью, что и бумага. Браузер так не умеет, он рисует текст отдельным слоем.
Плюс фотография разворота имеет фиксированный размер и не может вместить список переменной длины.

**Вывод, который стоил дня:** смешивать фотореализм и вёрстку нельзя — глаз ловит стык. Работает
либо всё фото, либо всё нарисовано браузером.

### Что выбрали

Пиксельное направление. Референс — присланный владельцем мокап (`docs/reference/`): пиксельная
иллюстрация стола по центру, весь контент в плоских панелях по краям. Текст не ложится на изогнутую
бумагу, поэтому не «плывёт», а арт и интерфейс говорят на одном визуальном языке.

### Решения

- **Шрифт Monocraft** — открытая перерисовка шрифта майнкрафта под OFL, с кириллицей. Настоящий
  шрифт Mojang проприетарный, клоны вроде Minecraftia лицензированы только для личного
  использования — на публичном сайте их ставить нельзя.
- **Масштаб только целый**, считается от ширины окна. Дробный мылит пиксель-арт.
- **Три типа ассетов**: тайл, рамка из девяти слоёв, предмет. Это ответ на вопрос адаптива: тайлы
  заполняют любой размер, рамки тянутся под содержимое, предметы только увеличиваются целым числом.
  Побочный выигрыш: книга из девятислойной рамки резиновая, и проблема «длинный список не влезает
  в фиксированную картинку» исчезает сама.
- **Ассеты хранятся маленькими**, в родном разрешении. Весь текущий набор заглушек — 1.7 КБ.

### Что сделано

Прибран проект под новое направление: удалены прототипы и фотоассеты, удалены компоненты кожаного
дневника (`Diary`, `DiaryCover`, `DiarySpread`, `DeskScene`), палитра и миксины переписаны на
пиксельные, добавлен расчёт целого масштаба с тестами, поставлен Monocraft, сгенерированы
временные ассеты-заглушки.

Уцелело без изменений: слой контента, логика отсчёта и выбора ивента, маршруты. 31 тест зелёный.

### Дальше

- [ ] Переписать спеку и план под пиксельное направление.
- [ ] Собрать сцену: панели, книга, предметы, навигация.
- [ ] **Ждём от владельца арт**: тайлы дерева и бумаги, рамки книги и панели, компас, часы, лампа,
      мелкий реквизит, арты игроков. Требования — в README, инструкция по изготовлению выдана.
      Первым делается компас как проба пайплайна.
- [ ] Статистика игроков: ванильный сервер её наружу не отдаёт, нужен экспорт в JSON на стороне
      сервера. Пока страницы наполняются заглушками, поля под API уже размечены.

---

## 2026-08-12 — Смена концепции: сайт-дневник

Каркас из записи ниже остаётся, но визуальная концепция меняется целиком: тёмно-зелёная тема и
обычный лендинг заменяются на кожаный дневник, лежащий на столе поверх карты.

### Что решили

Главная — закрытый дневник с тиснением и IP на бирке. Клик раскрывает его, внутри записи, ведущие
на разделы. Слева на столе компас (откроет карту), справа карманные часы с отсчётом до ивента.

Разделы: **Экипаж** (карточка на каждого игрока с артом и описанием), **Новости экспедиции**
(находки артефактов и события), **Устав экипажа** (правила). Магазин, вики и «как начать» из
навигации убираются.

Ключевые решения:

- Сцена стола постоянна, при навигации меняется только содержимое разворота. URL меняется на каждом
  переходе, кнопка «назад» работает.
- 3D тратится только на раскрытие обложки; смена разворотов — один поворачивающийся лист.
  Полноценное перелистывание каждой страницы отклонили: втрое дороже при малой прибавке к эффекту.
- При прямом заходе по ссылке дневник рендерится уже открытым, без интро.
- Графика — чистый CSS и SVG. Единственная растровая — арты игроков.
- Контент в JSON-файлах в репозитории, админку отложили.

Полная спецификация: [`docs/superpowers/specs/2026-08-12-diary-site-design.md`](docs/superpowers/specs/2026-08-12-diary-site-design.md).

### Дальше

- [ ] План реализации по спеке, затем сама вёрстка.
- [ ] Нужны от владельца: реальный IP и версия, ники и арты участников, текст устава, даты ивента.
- [ ] Арты участников генерируются нейросетью по скинам → единый стиль, формат 3:4, от 600×800,
      кладём в `public/crew/<nick>.png`. На развороте — рамка под старую фотокарточку.
- [ ] Шрифты (рукописный + антиква) положить в `public/fonts`.

---

## 2026-08-12 — Подготовка проекта к работе

### Что было

Пустой scaffold Vite + React 19 + TypeScript: демо-страница со счётчиком, логотипы Vite/React,
дефолтные стили. Реального кода сайта — нет.

### Что сделал

**1. Очистил boilerplate**

Удалено: `src/App.tsx`, `src/App.css`, `src/index.css`, `src/assets/` (react.svg, vite.svg,
hero.png), `public/favicon.svg` и `public/icons.svg` из шаблона.

**2. Зависимости**

- `react-router@8` — роутинг (в dependencies).
- `sass-embedded` — SCSS-модули (в devDependencies).

**3. Конфигурация**

- `vite.config.ts`: алиас `@` → `src/`; для SCSS прописан `loadPaths` на `src/styles` и
  `additionalData: '@use "core" as *'` — токены и миксины доступны в любом `.scss` без ручного импорта.
- `tsconfig.app.json`: `paths` для `@/*`. Без `baseUrl` — в TypeScript 6 он deprecated,
  поэтому путь пишем как `./src/*`.
- `index.html`: `lang="ru"`, title/description, OG-теги, `theme-color`.

**4. Каркас приложения**

```
src/app/routes.ts       ROUTES + NAV_ITEMS — единый источник путей
src/app/router.tsx      createBrowserRouter, все маршруты под MainLayout
src/app/App.tsx         RouterProvider
src/layouts/MainLayout  Header + <Outlet /> + Footer
```

Маршруты: `/`, `/start`, `/rules`, `/store`, `/map`, `/wiki`, `*` → 404.

**5. Компоненты и стили**

- `components/layout/Header` — sticky-шапка с лого и активной подсветкой ссылок (`NavLink`).
- `components/layout/Footer` — копирайт + соцсети.
- `components/ui/Button` — варианты `primary` / `secondary`.
- `components/ui/PageStub` — временная заглушка раздела со списком TODO по содержимому.
- `styles/`: `_variables.scss` (брейкпоинты, радиусы), `_mixins.scss` (`up`, `container`,
  `focus-ring`), `core.scss` (только `@forward`), `global.scss` (reset, тёмная тема, типографика).

Тема: тёмный «ночной лес» — фон `#0e1411`, акцент травяной `#5fbb63`, золото `#f0b232` для CTA.
Радиусы мелкие (2–8px) — сайт про блочный мир. Учтён `prefers-reduced-motion`.

**6. Контент-заглушки**

Главная — hero с названием, теглайном, IP-блоком и двумя кнопками. Остальные страницы — `PageStub`
со списком того, что на них должно появиться. Данные сервера собраны в `src/shared/config/site.ts`
(сейчас плейсхолдеры).

Временный favicon — пиксельный блок земли с травой (`public/favicon.svg`).

### Решения

| Вопрос | Решение | Почему |
| --- | --- | --- |
| Стили | SCSS-модули | выбор владельца проекта; изоляция классов + миксины |
| Роутинг | react-router, многостраничность | разделы правил/вики/магазина логичнее отдельными URL |
| Цвета | CSS-переменные, не SCSS | можно менять в рантайме, пригодится для тем |
| Данные сервера | один конфиг `site.ts` | IP и версия меняются часто, не хочется искать по коду |

### Проверено

- `npm run build` — проходит (98 модулей, css 5.75 kB, js 288.6 kB / 92 kB gzip).
- `npm run lint` — без ошибок.

---

## План дальше

### Ближайшее (нужно от владельца сервера)

- [ ] **Реальные данные** в `src/shared/config/site.ts`: IP, порт, версия, ссылки на Discord/Telegram.
- [ ] Определиться с концепцией: чисто ванильное выживание, RPG, мини-игры? От этого зависит структура лендинга.
- [ ] Лого и фирменный шрифт → заменить `--font-display` и favicon.

### Вёрстка

- [ ] Бургер-меню в шапке для мобильных (сейчас навигация скрыта до `lg`).
- [ ] Главная: hero-арт, блок «Почему мы», режимы игры, скриншоты, FAQ, CTA-футер.
- [ ] Кнопка «скопировать IP» с тостом.
- [ ] Страница «Как начать»: пошаговый гайд с картинками.
- [ ] Правила: оглавление с якорями, аккордеоны по разделам.
- [ ] Магазин: карточки привилегий, сравнение, дисклеймер про отсутствие pay-to-win.
- [ ] Карта: iframe Dynmap/BlueMap + переключатель миров.
- [ ] Вики: структура разделов, поиск.

### Функциональность

- [ ] Виджет онлайна: запрос статуса сервера (`api.mcstatus.io` или свой эндпоинт) + скелетон и фолбэк при недоступности.
- [ ] SEO: `react-helmet`-подобные теги на страницу, `sitemap.xml`, `robots.txt`.
- [ ] Аналитика (какая — решить).
- [ ] Приём платежей для магазина — выбрать провайдера, нужен бэкенд.

### Инфраструктура

- [ ] Prettier + конфиг форматирования (пока только ESLint).
- [ ] `git init` сделан, первый коммит — за владельцем.
- [ ] Деплой: статика на Vercel/Netlify/nginx. Для SPA-роутинга нужен fallback на `index.html`.
- [ ] Проверить Lighthouse и доступность после вёрстки лендинга.

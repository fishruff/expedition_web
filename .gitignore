# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Предпросмотр рамок из tools/gen_frames.py — служебный, смотрят и удаляют.
frame-*-preview.png

# Снимки из игры: во время сезона их пишет api, на дев-машине — npm run data:sample.
# В репозитории им делать нечего, а в продакшн-сборке — тем более.
public/data

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Сюда приезжают исходники ассетов до разбора.
incoming/

# Временное сравнение ассетов.
public/compare/

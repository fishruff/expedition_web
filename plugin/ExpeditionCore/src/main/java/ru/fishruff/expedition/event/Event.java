package ru.fishruff.expedition.event;

/**
 * Готовое событие: номер отдельно, тело строкой JSON.
 *
 * Номер вынесен наружу не для красоты. Сайт берёт номер записи в дневнике из номера
 * события, поэтому книгу надо пометить именно им — иначе повторный `/note` той же
 * книгой даст два одинаковых дневника. Доставать номер обратно из JSON пришлось бы
 * парсером, которого в плагине нет и не нужно.
 */
public record Event(String id, String json) {
}

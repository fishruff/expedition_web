// Типы Node — только здесь: приложение живёт в браузере и про `process` знать
// не должно, а часовой пояс выставляется до запуска тестов и больше нигде.
/// <reference types="node" />

// Часовой пояс тестов прибит гвоздями. Даты на сайте показываются местным
// временем, а «местное» у разработчика и в CI разное — без этой строки
// проверки на «сегодня» и «вчера» проходили бы или падали от машины.
process.env.TZ = 'Europe/Moscow'

import { afterEach } from 'vitest'
import { cleanup } from '@testing-library/react'

// Глобалы vitest выключены, поэтому автоочистку RTL включаем руками.
afterEach(cleanup)
